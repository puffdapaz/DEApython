# OpenMetadata Connection Configuration
version: "1.0"
project: "DEApython"
last_updated: "2026-02"
api_version: "v1"

host: "http://localhost:8585"
timeout: 30

# Service Names
services:
  bigquery_source:
    name: "basedosdados_bigquery_source"
    type: "bigquery"
    description: "Brazilian Public Data Compilation - BigQuery datalake"

  # python_etl:
  #   name: "python_etl_pipeline"
  #   type: "pipeline"
  #   description: "Python ETL scripts for medallion architecture"

  gcs_bronze:
    name: "gcs_bronze"
    type: "gcs"
    description: "Bronze folder at GCS bucket (deapython)"

  gcs_silver:
    name: "gcs_silver"
    type: "gcs"
    description: "Silver folder at GCS bucket (deapython)"

  gcs_gold:
    name: "gcs_gold"
    type: "gcs"
    description: "Gold folder at GCS bucket (deapython)"  
  
  neon_postgres:
    name: "postgres_service"
    type: "postgres"
    description: "Neon PostgreSQL with consume-ready data for analysis"
  
  # powerbi_dashboard:
  #   name: "pbi_dashboard"
  #   type: "powerbi"
  #   description: "Power BI dashboard for DEA visualization"

## Pipeline Names
# pipelines:
#   bronze_ingestion:
#     name: "ingest_bronze_data"
#     description: "Extract source tables from Basedosdados"
  
#   silver_processing:
#     name: "process_silver_data"
#     description: "Aggregate and flag completeness"
  
#   gold_modeling:
#     name: "model_gold_data"
#     description: "Apply DEA model and create analysis fields"